<p style="color: green"><%= notice %></p>

<% content_for :title, "Animals" %>

<div class="d-flex align-items-center mb-4">
  <h1>Animals</h1>
  <%= link_to "New animal", new_animal_path, class:"btn btn-primary ms-3"%>
</div>

<div id="animals">
  <% @animals.each do |animal| %>
    <%= render animal %>
  <% end %>
</div>